import 'package:flutter/material.dart';

/// FriendBook's own brand color, deliberately independent of Umbra's
/// shared navy/orange theme — the same justified exception pattern as
/// Unit Converter's fixed per-category accent colors (see the project
/// brief's Theming section): a branding choice, not a legibility fix,
/// so it doesn't route through colorScheme.
const Color kFriendBookBlue = Color(0xFF1877F2);
const Color kFriendBookBlueDark = Color(0xFF166FE0);
const Color kFriendBookOnBlue = Colors.white;
