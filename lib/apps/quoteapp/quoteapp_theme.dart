import 'package:flutter/material.dart';

/// QuoteApp's accent color — distinct from every other app's Home tile
/// color (MangaHub/Calculator use [AppColors.orange], FitLog/Unit
/// Converter use [Colors.teal]). Kept here as a single source of truth so
/// the Home tile and every screen inside this sub-app agree.
const Color quoteAppAccent = Colors.deepPurple;
